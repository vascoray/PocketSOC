[project]
name = "pocketsoc-lab-os"
version = "3.3.0"
description = "Rootless $0 Offline Mobile SOC Lab - Termux Pocket SOC"
readme = "README.md"
requires-python = ">=3.9"
authors = [{name="Vasco Romeo Yao Akpobi"}]
dependencies = ["flask"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
